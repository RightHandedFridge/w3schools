# Yo Waddup
All work from w3schools that I have completed

# C
## Compile
Any of the files can be compiled with:
``` gcc <file_name> -o <binary_name> ```

# Rust
## Compile 
Upon entering a directory with a /src/ directory the program can be ran with:
``` cargo run ```
